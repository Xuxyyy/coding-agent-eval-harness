# clamp

`src/clamp.js` exports `clamp(value, low, high)`. It returns `low` when the
value is below the range, `high` when it is above the range, and the original
value when it is inside the inclusive range.
